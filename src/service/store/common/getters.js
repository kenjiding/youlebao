export default {
  clientWidth(state) {
    return state.client.width;
  },

  clientHeight(state) {
    return state.client.height;
  },

  pageWidth(state) {
    return state.client.pageWidth;
  },

  pageHeight(state) {
    return state.client.pageHeight;
  },
};
