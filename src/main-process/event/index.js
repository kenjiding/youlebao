import { readCard, readEmpCard, readerCall } from './read-card';
import { zoomOut, resize, close } from './control-bar';
import { readIDCard, readIDCardCall } from './read-idcard';
import checkVersion from './check-version';
import getPrinterList from './get-printer-list';

export default {
  readCard,
  readEmpCard,
  readerCall,
  readIDCard,
  readIDCardCall,
  zoomOut,
  resize,
  close,
  checkVersion,
  getPrinterList,
};
